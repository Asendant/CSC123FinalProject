function displayControls() {
  background(245, 114, 71);

  text("Controls:", WIDTH / 2, HEIGHT / 2 - 100);
  text("Move: WASD or Up/Down/Left/Right Arrows", WIDTH / 2, HEIGHT / 2 - 50);
  text("Shoot: Left Click", WIDTH / 2, HEIGHT / 2);
}
