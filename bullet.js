class Bullet {
  constructor(sizeX, sizeY, vectorX, vectorY, originX, originY) {
    this.sizeX = sizeX;
    this.sizeY = sizeY;
    this.vectorX = vectorX;
    this.vectorY = vectorY;
    this.xPos = originX;
    this.yPos = originY;
  }
}
